---
  - name: Install Docker on Ubuntu Machines
    become: yes
    hosts: all

    tasks:
    - name: install docker
      apt:
        name: docker.io
        state: present
        update_cache: true
