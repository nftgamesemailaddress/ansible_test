---

- hosts: all
  become: true
  tasks:

  - name: Update repository index
    apt:
      update_cache: yes
  
  - name: Install apache2 package
    apt:
      name: apache2

  - name: Install a list of packages
    ansible.builtin.apt:
      pkg:
      - foo
      - foo-tools
